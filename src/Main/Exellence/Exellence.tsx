import React from 'react';
import stl from './Exellence.module.scss'

const Exellence = () => {
    return (
        <section className={stl.Exellence}>
            <div className={stl.container}>
                <div className={stl.imageWrapper}>
                </div>
            </div>
        </section>
    );
};

export default Exellence;